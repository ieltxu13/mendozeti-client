import { FiltroComprobantePipe } from './filtro-comprobante.pipe';

describe('FiltroComprobantePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroComprobantePipe();
    expect(pipe).toBeTruthy();
  });
});
