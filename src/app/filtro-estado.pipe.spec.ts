import { FiltroEstadoPipe } from './filtro-estado.pipe';

describe('FiltroEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
